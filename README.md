## Ethernaut Level 37 Impersonator Two

This is a solution for Ethernaut Level 37 Impersonator Two. We use `recover_and_sign.ts` to calculate the signer's private key. Then run `Solution.s.sol` to drain the instance's balance.
